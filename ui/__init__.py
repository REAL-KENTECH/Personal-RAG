"""Cross-view UI primitives.

- ``helpers`` — the tiny HTML-injecting helpers (``_chip``, ``_section``,
  ``_empty``) that views compose for consistent visual rhythm.
- ``widgets`` — ``model_picker``, the per-provider dropdown with a
  "직접 입력" sentinel and an instant / pending-apply toggle.
- ``sidebar`` — ``render_sidebar``, the persistent left rail (brand,
  new-chat button, navigation, saved conversations, status, user).
"""
