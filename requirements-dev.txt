# Development / test dependencies.
# Runtime requirements are split into requirements.txt + requirements-extras.txt
# and shipped to Streamlit Cloud. Dev deps stay separate so deploys don't pull
# pytest just to run the app.
#
#   pip install -r requirements-dev.txt   # in your local venv
#   pytest                                # run the test suite

pytest>=8.0
