# 실습 전용 의존성. 본 프로젝트 메인 앱(../requirements.txt)과는 별도다.

# LangChain — text_splitters 는 별도 패키지로 분리됐다 (LangChain 0.3 이후)
langchain>=0.3
langchain-community>=0.3
langchain-text-splitters>=0.3

# OpenAI 경로
langchain-openai>=0.2

# Hugging Face 경로 — 오픈 모델 쓸 때만 실제로 필요하지만 함께 설치해두면 편하다
langchain-huggingface>=0.1
sentence-transformers>=2.7

# 벡터 DB
faiss-cpu>=1.7

# PDF 파서
pypdf>=4.0

# Streamlit (step7 이후)
streamlit>=1.40

# .env 자동 로드
python-dotenv>=1.0
