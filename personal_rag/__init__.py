"""Personal RAG — modularized package.

The original single-file implementation lives in app.py at the project root.
This package gradually re-homes that code into focused submodules so a new
contributor can navigate by responsibility instead of scrolling 5000+ lines.
"""
